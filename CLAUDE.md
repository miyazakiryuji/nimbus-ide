# Nimbus — 作業ルール

Nimbus 固有のルールだけを置く。VS Code コードベース側の作法は
[`.github/copilot-instructions.md`](.github/copilot-instructions.md)（`.claude/CLAUDE.md` はそこへのシンボリックリンク）。
開発の原則は [`README.md`](README.md)、やること・やりたいことは [`tasks.md`](tasks.md)。

## 既存の機能を壊さない（最優先）

**動いているものを壊す修正は、原則として行わない。** Nimbus は「足していく」開発であって、
「作り直す」開発ではない。既に入っている機能 — F1〜F6、テーマ、IntelliJ 由来で足していく機能
（スクラッチファイルなど）— の振る舞いを、頼まれてもいないのに変えない。

- **足すのは自由。既にあるものの改変・削除は、明示的に頼まれたときだけ。**
  「ついでに直しておきました」「こちらの方が綺麗なので変えました」は、してはいけない
- 共有モジュール（`sanitizer.ts` / `permissions.ts` / `core/*`）の**既存の関数シグネチャ・export を
  変えない**。足すのは可。変える必要があるときは呼び出し元をすべて直し、既存テストを全部通す
- コア（`src/vs/**`）は `// --- Start Nimbus ---` ブロックの中だけ。upstream の行を書き換えない
- 手を入れる前に既存テストを走らせて **Before の結果を控え、After で同じだけ通ることを確認する**
- どうしても既存の振る舞いを変えるべきだと思ったら、**黙って変えずに提案する**。
  変えると決まったら、仕様書・確認項目・`tasks.md` を同じコミットで揃える
- 消す判断をしたときは、理由を `tasks.md` の「保留・やらないと決めたこと」に 1 行残す

複数の AI が並行で触るので、「壊れていないのに直す」は他のセッションの前提を崩す。
リファクタしたくなったら、それ自体を `tasks.md` のタスクとして立てて、合意してから着手する。

## 作業の前に、板へ書き出す（毎回）

**手を動かす前に [`tasks.md`](tasks.md) へタスクとして書き出してから着手する。** 頼まれごとを
そのまま実装し始めない。板は複数の AI が共有する唯一の調整面なので、書き出していない作業は
**他のセッションから見えない**。実際に、同じ土台（セッションの永続化）を 2 セッションが
別々に作りかけて正面衝突した。

- 依頼を受けたら、まず 1 行でよいので板に起こす。番号は既存の最大 ＋1
- 着手したら「進行中」へ移し、担当（`@session-x`）と日付を添える
- **着手前に板を読み、同じ範囲に `@` が付いていないか確かめる**。付いていたら、
  自分で始めずに利用者へ確認する
- 終わったら `[x]` にして、仕様書へのリンクと日付を残す

### ファイルは板で予約してから触る（毎回）

**タスクを板に起こしたら、編集する予定のファイルを板の「作業予約」の節へ 1 行で書き出してから
手を入れる。作業が終わったら、その行を自分で消す（＝解放）。** 札（`@session-x`）は「誰が何を
やっているか」までしか伝えない。同じ作業ツリーを共有しているので、同じファイルを 2 セッションが
同時に編集すると、編集の取り違えと巻き込みコミットが起きる。ファイル単位の予約がそれを防ぐ。

書式（1 予約 = 1 行。行頭の `- 🔒` が「いま握っている」印。時刻は開始ではなく**最終更新 = 心拍**）:

```
- 🔒 @session-x | T-123 | 2026-08-25 20:00 | extensions/nimbus/src/extension.ts, extensions/nimbus/media/cockpit.js
```

- **着手前**: `node nimbus/scripts/board.mjs` か板の「作業予約」の節で、触りたいファイルが他の
  `- 🔒` 行に入っていないか確かめる。**読むだけなら予約は要らない**。要るのは編集（Write / Edit）から
- **空いていれば**: 自分の行を足してから編集を始める。**新しく作るファイルも予約に書く**
  （置き場所の取り合いも衝突になる）
- **握られていたら**: 始めない。解放を待つか、重ならない別のタスクへ移るか、利用者に確認する。
  待つときは tasks.md を数分おきに読み直す — バックグラウンドで
  `while grep -q '^- 🔒.*<path>' tasks.md; do sleep 60; done` を回してもよい。
  30 分待っても解放されないときは、次の節「引き継ぎの手順」で持ち主の生死を確かめる
  （落ちていれば引き継げる。生きていそうなら利用者へ報告）
- **心拍**: コミットと push のたびに、自分の 🔒 行の時刻を今に書き直す（済んだファイルはそこで外す）。
  この時刻が止まって見えることが、「持ち主のセッションが落ちた」を機械で見分ける材料になる
- **解放**: コミットと push が済んだら自分の 🔒 行を消す。タスクを `[x]` にするのと同じコミットでよい。
  **途中で作業を止めて利用者へ返すときも、握ったままにしない**（残りをタスク側に書いてから行を消す）
- 自分の行は途中で直してよい（ファイルを足す・済んだぶんを減らす）。**他人の 🔒 行に触ってよいのは、
  次の節の手順で「落ちた」と確かめて引き継ぐときだけ**
- ほぼ全機能が触る共有ファイル（`extension.ts`・`package.json`・`*.nls.json` など）は**長時間握らない**
  — 触る直前に自分の行へ足し、コミットしたらすぐ外す
- `tasks.md` 自体は予約の対象外（全員が書く調整面）。ただし編集してよいのは自分の行と自分のタスクだけ

### 止まったタスクは、続きから再開できる形にする（毎回）

**セッションはいつでも落ちる。落ちたところから、別のセッション（明日の自分を含む）が続きを
再開できないなら、札も予約もただの通行止めになる。** 守るのは 3 つ:

1. **再開できる形で刻む** — コミットは意味の通る最小単位で切って push し、一気に終わらないタスクには
   **コミットのたびに進み具合を書き足す** — `済: ①②（abc1234）/ 残: ③ / 次: 〜` の 1〜2 行でよい。
   頭の中にしか無い「残り」は、落ちた瞬間に消える
2. **止まるときは残りを書いてから** — 利用者へ返す・中断するときは、進み具合を書いて 🔒 を外す
3. **落ちたセッションの分は、放置せず引き継ぐ** — 次の手順で

**引き継ぎの手順**（`board.mjs` が ⚠ を出したら / 解放待ちが 30 分を超えたら）:

1. **生死を確かめる** — 心拍（🔒 の時刻）が 60 分より古く、**かつ**予約されたファイルの更新も
   30 分以上止まっていたら、持ち主は落ちたとみなしてよい（`node nimbus/scripts/board.mjs` が
   両方を測って ⚠ を出す）。ファイルが最近動いているなら生きている — 触らず、待つか利用者へ。
   **利用者が「引き継いで / 続けて」と言ったときは、この確認ごと飛ばしてよい**
2. **札と予約を引き取る** — 相手の 🔒 行を自分の行に書き換え、末尾に `（@session-x から引き継ぎ）`と
   添える。タスク側にも `→ @session-y 引き継ぎ 2026-08-26` を書き足す（前任の記録は消さない）
3. **前任の作りかけを読んでから続ける** — push 済みの分は `git log --oneline --grep 'T-123'`、
   未コミットの分は `git diff -- <予約ファイル>`。**未コミットの作りかけは捨てずに、そこから続きを
   書く**。使いものにならないと判断しても勝手に消さず、利用者に確認する
4. **タスクの進み具合（済 / 残 / 次）から再開する** — 書かれていなければ、仕様書と diff から現在地を
   推定し、推定した現在地をタスクに書いてから進める

## Git — 細かくコミットし、必ず push する

- **コミットとプッシュは細かく、必ず実施する。** 意味の通る単位ができた時点でコミットし、溜め込まない
- 1 コミット = 1 つの意図。無関係な変更を混ぜない
- **コミットしたら、その都度 push する** — `git pull --rebase origin nimbus` → `git push`。
  複数の AI が同じブランチ `nimbus` を同時に触るので、ローカルに溜めるほど競合が解けなくなる
- 他のセッションの未コミット変更を `git stash` / `git checkout --` / `git reset --hard` で消さない
- 公開リポジトリ（`miyazakiryuji/nimbus-ide`）なので、個人情報・実名・資格情報を含めない

## 直したら、戻らないようにする（毎回）

**直した不具合には回帰テストを 1 本足す。** 回帰は実際に起きている（T-091 は一度直したあとに戻り、
`3d3be9a5a81` で直し直した）。並行セッションが古い前提で書き直す・テストが無いまま緑になる・
upstream 追従でコアの Nimbus ブロックが落ちる、の 3 つが原因なので、対策もその 3 つに当てる。

- **テストの中に T 番号を書く**（見出しコメントでよい）。どの修正を守っているのかが分からないと、
  後から「これは消してよいテストか」を判断できない
- いま何が守られていないかは `node nimbus/scripts/regression-guard.mjs` で見る
- **数が減っていないか**は `bash nimbus/scripts/test.sh degrade` で見る（基準は
  `nimbus/tests/baseline.json`）。ある程度実装したら走らせる。**意図して消したときは、
  変更と同じコミットで `node nimbus/scripts/degrade.mjs record`** — 基準の動きが diff に残る
- コア（`src/vs/**`）の変更が落ちていないかは `node nimbus/scripts/doctor.mjs` の台帳照合（赤で落ちる）
- 画面で見える振る舞いは、固めた `.app` で**該当機能のケース**を走らせて確かめる（次の節。
  全ケースを回すのは利用者が「総合試験」と言ったときとリリース前だけ）

## 修正したらアプリとして固める（毎回）

**コードに手を入れたら、その都度デスクトップアプリとして固め直し、固めた `.app` で動くことまで
確かめる。** dev 起動（`./scripts/code.sh`）で動いたことは、パッケージ版で動く証拠にならない
（同梱漏れ・`product.json`・NLS・拡張のバンドルは固めて初めて出る）。

```bash
bash nimbus/scripts/package-app.sh --copy /tmp/nimbus-gui-app   # 順番待ちして固め、写しを作る
bash nimbus/branding/smoke-packaged.sh                          # 身元・起動・webview のスモーク
NIMBUS_APP=/tmp/nimbus-gui-app/Nimbus.app node nimbus/tests/gui/run.mjs --packaged --only <ケース名>
```

**GUI テストは、実装した機能のケースだけ走らせる**（T-325・利用者指示 2026-08-25）。
毎回の修正で全ケースを回すと 1 修正が重すぎる。`--only` はケース名・ファイル名の部分一致
（例: `--only 47-` / `--only テーマ`）。1 回に 1 語なので、複数のケースは共通の語で当てるか、
コマンドを分けて走らせる。**関係が疑わしいケースは含める** — 絞りすぎて壊れに気づけないのは本末転倒。
**全ケースのフル実行（総合試験）は、利用者が「総合試験を実施して」と言ったときと、
リリース前にだけ回す** — `--only` を外した GUI 全件（`--untrusted` の別起動ぶんも）＋
モジュールテスト＋ドクター。手順は nimbus-test スキルの「総合試験」の節。

**`npm run gulp vscode-darwin-arm64` を直に叩かない**（T-276）。出力先は 1 つしかないので、
別のセッションと重なると**相手が消した途中のファイルを踏んで落ちる**。
`package-app.sh` は順番待ちをして、固めたあとに自分用の写しを作る —
写しを見て GUI テストを走らせれば、後から始まった別のビルドに壊されない。

- **対象**: `src/vs/**` / `extensions/**` / `build/**` / `product.json` / `nimbus/branding/**` を
  触ったとき。ドキュメント・仕様書・`tasks.md` だけの修正は固め直さなくてよい
- **タイミング**: コミット → push のあと。コミット単位で固め、溜め込まない
- **完了の条件**: 「実装した」ではなく「**固めた `.app` で押したら動いた**」までで 1 つの修正が終わる。
  GUI テストは存在確認で止めず、実際に押して実行されるところまで確かめる
- 固め直しで壊れていたら、次の作業へ進まずそこで直す。ビルドが通らない状態で放置しない
- 手順の詳細・dmg 化・署名の注意は [`nimbus/docs/specs/distribution.md`](nimbus/docs/specs/distribution.md)

## コマンドを足したら、入口も足す（毎回）

**コマンド名でしか引けない機能は、名前を知っている人にしか存在しない。** 実際に
「実装したはずなのに無い」という報告が続けて 4 件出た（新しいセッション T-290 / 右半分 T-292 /
セッション一覧 T-293 / 全画面とタブで開く T-289）。どれもコマンドは在り、入口だけが無かった。

- `contributes.commands` に足したら、**同じコミットで**押せる場所も足す —
  `menus.view/title`（面のタイトル）か、webview の中のボタン
- タイトルに置くコマンドには **`icon` を必ず付ける**（無いと歯抜けのまま並ぶ）
- **全画面（エディタタブ）でも押せるか**を確かめる。全画面はサイドバーごと畳むので、
  `view/title` だけに置くと入口がゼロになる。`editor/title` にも同じものを置く
- 守りは `src/test/cockpitMenus.test.ts`（並びとアイコンの有無を固定している）

## 実装したら記録も直す

実装と**同じコミットで** `nimbus/docs/specs/` の仕様書と [`tasks.md`](tasks.md) を直す。
コア（`src/vs/**`）に触ったら `nimbus/docs/core-changes.md` にも記録する。
（詳細は README「実装したら仕様書を直す」「複数の AI で並行開発する」）
