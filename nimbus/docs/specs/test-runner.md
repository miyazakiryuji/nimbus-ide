# テストランナー連携（T-039 / T-108 / T-109）

テストが落ちたとき、人は出力を目で追って「**どのテストが**」「**どこで**」「**なぜ**」を
組み立て直している。VS Code の Test Explorer はその 3 つを**構造として**持っているので、
そのまま渡せば読み解く手間が消える。ターミナルの出力（[T-169](terminal-capture.md)）より正確で短い。

テストランナー拡張が何であっても（Jest / Vitest / pytest / Dart / Go …）同じ形で取れるのが要点。
**フォークにした旨味が一番出る場所**（`tasks.md` 選ぶ基準 2）。

## 振る舞い

1. Test Explorer でテストを走らせ、**失敗して終わる**
2. 「テストが 3 件失敗しました」という通知が出る
3. **「セッションに投入」**を押すと、コックピットが開いて、
   落ちたテストの名前（スイートから辿った形）・ファイルと行・メッセージが送られる
4. 通知を閉じてしまっても、コマンドパレットの
   **「直近に失敗したテストをセッションに投入」**（`nimbus.sendLastTestFailure`）で拾い直せる
5. 「今後は知らせない」を押すと設定が切り替わる

## 実装

- `extensions/nimbus/src/core/testFailures.ts` — 木を歩いて失敗を取り出し、文にする（純関数）
- `extensions/nimbus/src/testWatcher.ts` — `vscode.tests.onDidChangeTestResults` の口
- `extensions/nimbus/src/extension.ts` — `TestWatcher` の生成とコマンド登録
- テスト: `extensions/nimbus/src/test/testFailures.test.ts`

## 決めたこと

**`testObserver`（提案 API）を使う。** テスト結果を読む API は安定版に無い。組み込み拡張なので
`enabledApiProposals` で有効にできる（`extensions/nimbus/package.json` と `tsconfig.json`）。
ただし**提案 API はいつか消える**ので、`onDidChangeTestResults` が無いときは
購読せずに黙って何もしない。拡張の有効化そのものを壊さないことを優先する。

**失敗した末端だけを報告する。** スイートも「失敗」として立つので、そのまま拾うと
「session」「session › イベントを正規化する」が二重に並ぶ。中に失敗を含む節は飛ばす。

**名前は親から辿って組み立てる。** `session › イベントを正規化する`。
テスト名だけでは、同名のテストが複数あるプロジェクトで特定できない。

**行は 1 起点に戻す。** VS Code の `Range` は 0 起点だが、エディタの表示と `Read` の出力は 1 起点。
モデルから見える面はそちらに揃える（[lsp-tools](lsp-tools.md) と同じ判断）。

**400ms 待ってから読む。** 実行中にも結果の配列が更新されることがあるので、落ち着いてから 1 回だけ出す。
同じ実行（`completedAt`）は二度知らせない。

**上限は 10 件・1 件あたりメッセージ 3 つ・1,200 文字。** 100 件落ちたときに全部渡しても読めない。
落とした分は「…他 N 件は省略しました」と明示する。

**通知は警告で出す。** 情報通知は数秒で消えるので、押す前に消えたら「ワンクリック」にならない。

## 回帰の検知（T-108）

**「もともと落ちている」と「いま壊した」はまったく別の話。** 混ぜて渡すと、直近の変更と
関係ないテストの修正を始めてしまう。

- 実行のたびに「**通った末端テストの名前**」を覚えておく（スイートは数えない。
  中の 1 つが落ちればスイートも落ちるので、比較の単位にならない）
- 次の実行で、そこに載っていたテストが落ちたら**回帰**として印を付ける
- 回帰は**上限で切られる前に先頭へ回す**。10 件目以降に埋もれたら意味がない
- 通知の見出しも変わる — 「前回通っていたテストが 1 件落ちました（失敗は全部で 3 件）」
- 投入する文の冒頭でも「うち N 件は**前回まで通っていた**もので、直近の変更が壊した可能性が高い」と言う

比較の基準は**そのウィンドウを開いている間**だけ持つ（永続化しない）。
最初の 1 回は基準が無いので、回帰は出ない。

## カバレッジ差分（T-109）

全体のカバレッジ率は、上がっても下がっても誰も動かない数字になりがち。効くのは
「**この変更で増えた行が、テストされているか**」だけ。

コマンド **「この変更のカバレッジを見る」**（`nimbus.coverageDiff`）で:

1. `git diff -U0 HEAD` から**足した行**を取り出す（`-U0` なら hunk の見出しだけで分かる）
2. 直近のテスト実行の `getDetailedCoverage` を、そのファイルについて引く
3. 足した行のうち**実行されなかった行**を出す
4. 「テストを書かせる」を押すと、その行を名指ししてセッションへ投入する

**計測対象でない行（空行・コメント・型だけの行）は未カバー扱いにしない。**
混ぜると、直しようのない指摘で埋まって誰も読まなくなる。
行番号は `14–15, 22–24` の形に畳む。

カバレッジを報告していない実行しか無ければ、「カバレッジつきで実行してください」と言って終わる
（黙って空の結果を出さない）。

## 失敗テスト起点の開発（T-107）

「実装してからテストを書く」と、テストは**実装に合わせて**書かれる。それでは
「動いた気がする」を潰せない。**先に赤を見てから緑にする**を、頼み方の側で固定する。

コマンド **「失敗するテストから始める」**（`nimbus.startFromFailingTest`）で、
作るものを 1 行入力すると、次の順序を明示した指示が送られる:

1. 期待する振る舞いをテストに書く（実装はまだ書かない）
2. 走らせて**落ちること**を確かめる（落ちなければ、テストが振る舞いを捉えていない）
3. 通る最小の実装を書く
4. もう一度走らせて緑を確かめる

**「テストを実装に合わせて書き換えないでください」を毎回入れる。** これが抜けると、
緑にするためにテストの方が緩められる。

赤 → 緑になった瞬間は、テスト結果の購読側が拾って**一度だけ**知らせる
（毎回「全部通りました」を出しても読まれない）。

## 影響を受けるテストだけを走らせる（T-180）

全部回すのは遅いというより、**確認が習慣にならない**のが問題。関係するぶんだけなら
数秒で終わるので、直すたびに回せる。

コマンド **「変更に関係するテストだけを走らせる」**（`nimbus.runImpactedTests`）:

1. `git diff --name-only HEAD` で変更されたファイルを集める
2. 変更されたのがテストなら、それ自体を走らせる
3. 実装ファイルは、**それを参照しているテスト**（言語サーバーの参照検索）に置き換える
4. **走らせる前に一覧を見せる**（既定は全部にチェック）。何が走るか分からないまま実行させない
5. 選ばれたファイルを `testing.runCurrentFile` で 1 つずつ走らせる

**テストが見つからなかった変更は「見つからなかった」と出す。** 黙って落とすと、
「関係するテストは 0 件でした」が安心の材料になってしまう。

参照検索は 1 ファイルあたり先頭 15 シンボルまで。深追いしても当たりはほとんど増えない。

## 設定

| 設定 | 既定 | 意味 |
| --- | --- | --- |
| `nimbus.tests.captureFailures` | `true` | 失敗したテストを知らせる |
| `nimbus.tests.maxFailures` | `10` | 投入する失敗の件数 |

## 確認すること

- [ ] 画面確認: Test Explorer からテストを走らせて落とすと通知が出る
- [x] 投入する文が名前・場所・メッセージを含む（行は 1 起点） — `投入する文は名前・場所・メッセージを含み、行は 1 起点に戻す` （`testFailures.test.ts`）。※コックピットが開くところは画面確認
- [x] 全部通ったときは何も組み立てない — `失敗が無ければ何も組み立てない` （`testFailures.test.ts`）。※通知そのものは画面確認
- [x] スイートとテストが二重に並ばない — `失敗した末端だけを、親から辿った名前で集める` （`testFailures.test.ts`）
- [ ] 画面確認: 同じ実行で通知が二度出ない
- [x] 上限を超えた分は落とすが総数は残す — `上限を超えた分は落とすが、総数は残す` / `見出しは件数を言う` （`testFailures.test.ts`）
- [ ] 画面確認: 通知を閉じたあと `nimbus.sendLastTestFailure` で投入できる
- [ ] 画面確認: 「今後は知らせない」で通知が止まる
- [x] 前回通っていた失敗に印が付く（T-108） — `前回通っていた失敗には印が付き、上限で切られる前に先頭へ回る` （`testFailures.test.ts`）
- [x] 元から落ちているものには印が付かない — `通った末端だけを次回の基準にする（スイートは数えない）` （`testFailures.test.ts`）
- [x] 回帰は上限で切られる前に先頭へ回る — `前回通っていた失敗には印が付き、上限で切られる前に先頭へ回る` （`testFailures.test.ts`）
- [ ] 画面確認: カバレッジつきで実行してからコマンドを叩くと、未カバーの行が出る（T-109）
- [ ] 画面確認: カバレッジ無しの実行しか無いときは「カバレッジつきで実行してください」と出る
- [ ] 画面確認: 全部通っていれば「すべてテストで実行されています」と出る
- [x] 先に落ちるテストを書かせる文が、順序と禁止事項を明示する（T-107） — `先に落ちるテストを書かせる文は、順序と禁止事項を明示する` （`testFailures.test.ts`）
- [ ] 画面確認: 落ちていたテストが全部通ると「赤 → 緑になりました」が一度だけ出る
- [ ] 画面確認: 最初から緑のときは何も出ない
- [x] 実装ファイルが、それを参照しているテストに置き換わる（T-180） — `実装ファイルは、それを参照しているテストに置き換える` （`impactedTests.test.ts`）
- [x] 変更されたテストは、それ自体を走らせる — `変更されたテストは、それ自体を走らせる` （`impactedTests.test.ts`）
- [x] テストが見つからない変更は黙って落とさない — `テストが見つからない変更は uncovered に残す（黙って落とさない）` / `説明は件数から始め、テストの無い変更も添える` （`impactedTests.test.ts`）
- [ ] 画面確認: 一覧で外したファイルは走らない
- [ ] 画面確認: `testObserver` が無効な状態でも拡張が正常に有効化される（ログに 1 行残るだけ）

## 残っていること

- **CI 連携は入っていない。** 「CI が赤くなったらログを取りに行く」は T-131 として別に立っている
- カバレッジ（`getDetailedCoverage`）は使っていない（T-109）
- 落ちたテストを**選んで**投入する UI は無い（今は上位 N 件をまとめて渡す）
