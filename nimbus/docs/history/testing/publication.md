# 公開作業 確認チェックリスト — Public GitHub リポジトリ・v0.1.0

- 実施日: 2026-08-11
- 指示: Public 作成可・細かく Push・**個人的な情報は絶対に上げない**・ダウンロード方法を丁寧に README へ

## 1. 個人情報監査（公開前必須）

| #   | 項目                                                                         | 結果 | 確認方法                                           |
| --- | ---------------------------------------------------------------------------- | ---- | -------------------------------------------------- |
| A-1 | 追跡ファイル全量 grep（ユーザー名・実名・メール・ホームパス）                | OK   | `git grep -iE` — 該当はテスト内ダミーパスのみ      |
| A-2 | **全リビジョン**の grep スイープ（履歴に残る過去版も含む）                   | OK   | `git grep ... $(git rev-list --all)` → 0 件        |
| A-3 | コミット作者を GitHub ユーザー名＋noreply メールに統一（実名を履歴から除去） | OK   | filter-branch 後 `git log --format="%an <%ae>"`    |
| A-4 | 旧スクリーンショット（実パス・個人 MCP ツール名入り）の blob を履歴から除去  | OK   | index-filter＋gc 後、blob 不存在を cat-file で確認 |
| A-5 | スクリーンショットをサニタイズ環境で撮り直し・目視検品                       | OK   | 下記 2 節                                          |

## 2. スクリーンショットのサニタイズ手順（再現レシピ）

隔離環境: `NIMBUS_USERDATA=/tmp/nimbus-userdata NIMBUS_CLAUDEMD_HOME=/tmp/nimbus-home NIMBUS_SMOKE=1 NIMBUS_SMOKE_SAFE=1 NIMBUS_SMOKE_CWD=/tmp/nimbus-demo`

| #   | 検品項目                                                          | 結果 | 備考                                             |
| --- | ----------------------------------------------------------------- | ---- | ------------------------------------------------ |
| B-1 | cwd・CLAUDE.md 階層・セッション一覧に実パスが写っていない         | OK   | /tmp/nimbus-demo のみ                            |
| B-2 | ツール一覧がコアのみ（個人の MCP コネクタ・プラグイン名が出ない） | OK   | settingSources:[]＋strictMcpConfig（実測で確立） |
| B-3 | ツール出力に OS ユーザー名が写っていない（ls の所有者列等）       | OK   | 初回 NG → ツール不使用プロンプトに変更して再撮   |
| B-4 | 課金モード表示が正しい（サブスク利用）                            | OK   | 目視                                             |

## 3. リポジトリ・リリース

| #   | 項目                                                                  | 結果 | 確認方法                                                                                                                                                      |
| --- | --------------------------------------------------------------------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C-1 | Public リポジトリ作成（miyazakiryuji/nimbus）＋ main push             | OK   | `gh repo view` → PUBLIC・リモート 16+ コミット                                                                                                                |
| C-2 | 以後コミットごとに push する運用                                      | OK   | 運用ルール（メモリにも記録）                                                                                                                                  |
| C-3 | README にダウンロード手順（前提・dmg 導入・Gatekeeper・ソースビルド） | OK   | README「Download」節                                                                                                                                          |
| C-4 | v0.1.0 リリース作成＋dmg 添付＋パッケージ版スモーク                   | OK   | releases/tag/v0.1.0。パッケージ版で実 SDK 1 往復＋DB 記録を確認（init/user/assistant/turn-result）。署名は adhoc のみ（実名なし）。dmg SHA256=e8e05fab…e1ca0e |

## NG 記録と再実施

- **B-2/B-3 初回 NG**: HOME 退避方式は Electron からの Keychain 認証が通らず「Not logged in」、かつ userData が HOME に追従せず旧セッション一覧が写り込み。→ 実 HOME＋`NIMBUS_USERDATA` 分離＋`settingSources:[]` 方式に切替（素の SDK プローブで認証・プラグイン 0 を実測確認）。ls 出力の所有者列に OS ユーザー名が写る問題はツール不使用プロンプトで回避し、全項目再検品で OK
- **手順ミス（復旧済み）**: filter-branch が作業ツリーを書き換え後 HEAD に同期するため、未コミット扱いになった新スクリーンショットがディスクから消失 → 確立済みレシピで再撮影・再検品・コミット。教訓: filter-branch は「全てコミット済み・退避済み」で実行する
- **C-4 初回 NG（2 件・パッケージ検証が検出）**: (1) 既定ビルドの署名 identity に開発者の実名（Apple Development 証明書名）が埋め込まれ、`codesign -dv` で露出 → `identity: null`（ad-hoc）に変更し、署名が `adhoc` のみになることを実測確認。(2) パッケージ版で `spawn ENOTDIR` — Agent SDK の同梱 CLI バイナリは asar 内から実行不可 → `asarUnpack: node_modules/@anthropic-ai/**` で解決。**「修正のたびにアプリとして固める」運用が初回から本番相当の欠陥を 2 件捕獲**
