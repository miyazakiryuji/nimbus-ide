# CLAUDE.md タブ 確認チェックリスト（T-008 / T-009）

仕様: [`nimbus/docs/specs/claude-md.md`](../specs/claude-md.md)

## 1. 自動テスト（`node --test`）

`extensions/nimbus/src/test/claudeMdDoc.test.ts`（14 件・全通過 2026-08-13）

| # | 項目 | 結果 |
| --- | --- | --- |
| A-1 | 見出しでセクションに分かれ、行番号を保つ | OK |
| A-2 | コードブロック内の `#` を見出しにしない | OK |
| A-3 | 見出しより前の本文も 1 つのセクションになる | OK |
| A-4 | 空のファイルはセクションを持たない | OK |
| A-5 | 階層を判定する（プロジェクト / 継承 / ユーザー） | OK |
| A-6 | 表示名がプロジェクト相対・ホームは `~` に短縮される | OK |
| A-7 | セクションを末尾に足せる | OK |
| A-8 | 同じ見出しがあるときは足さず、その行を返す | OK |
| A-9 | 空のファイルにも足せる | OK |
| A-10 | ひな形が見出し・説明・本文を持つ | OK |
| A-11 | 重複した見出しと中身の無い節を指摘する | OK |
| A-12 | 同じ行の重複を指摘する（短い行・コードブロックは見ない） | OK |
| A-13 | 長すぎるファイルだけを長すぎると言う | OK |
| A-14 | トークン数がおおよそ文字数の 2/3 になる | OK |

実行: `cd extensions/nimbus && npx tsc -p tsconfig.json && node --test "out/test/*.test.js"`

## 2. 画面確認（未実施）

開発ビルド（`./scripts/code.sh`）で、Nimbus のサイドバー → 「CLAUDE.md」。

- [ ] B-1 効いている CLAUDE.md が並び、出どころ（プロジェクト／親フォルダから継承／ユーザー設定）が
      説明として出る
- [ ] B-2 ファイル名がプロジェクト相対で出る。ホーム以下は `~/…` に短縮されている
- [ ] B-3 ファイルをクリックすると、その CLAUDE.md がエディタで開く
- [ ] B-4 展開すると見出しが並ぶ。クリックすると**その見出しの行**に着地する
- [ ] B-5 コードブロック内の `#` が節として並んでいない（このリポジトリの `CLAUDE.md` で確認できる）
- [ ] B-6 「＋」でひな形を選ぶと末尾に足され、その行が開く
- [ ] B-7 既にある見出しを選ぶと「既にあります」と出て、重複せずその節が開く
- [ ] B-8 CLAUDE.md が無いフォルダで「＋」を押すと、フォルダ直下に作られる
- [ ] B-9 「更新」で一覧が作り直される（ファイルを手で足したあとに反映される）
- [ ] B-10 ファイルの説明に「約 N トークン」が出る
- [ ] B-11 指摘があるファイルでは、節より先に「N 件の指摘」が出る。クリックで該当行へ飛ぶ
- [ ] B-12 指摘が無いファイルには「指摘」の行が出ない
- [ ] B-13 テーマを Nimbus Light に切り替えても文字色・アイコンが読める

## 3. パッケージ版スモーク（未実施）

- [ ] C-1 `npm run gulp vscode-darwin-arm64` で固めた `.app` でも B-1 〜 B-4 が同じように動く
